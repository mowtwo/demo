import Router from "preact-router";
import OpenFilePicker from "./openFilePicker";

export default (props) => {
  return (
    <div
      style={{
        marginTop: "56px",
      }}
    >
    <h1>Demo</h1>
    </div>
  );
};
